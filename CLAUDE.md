# Fan-Out / Fan-In Orchestrator Guidelines

이 문서는 대규모 기능 구현, 전면 리팩터링, 다중 모듈 테스트 및 코드 분석 시 Claude Code 환경에서 **Fan-Out / Fan-In 오케스트레이션 패턴**을 강제하기 위한 지침입니다.

---

## 1. 역할 정의 (Core Roles)

* **Orchestrator (메인 세션)**
  - 작업을 분석하고 독립 실행 가능한 단위(Sub-tasks)로 분해합니다.
  - 서브태스크 간 파일 충돌 가능성을 사전에 차단(Isolation)합니다.
  - 서브에이전트들의 산출물을 취합, 통합 검증(Fan-In)하고 최종 납품합니다.
* **Worker Agents (서브태스크 에이전트)**
  - 할당받은 단일 작업과 대상 디렉터리/파일에만 집중합니다.
  - 지정되지 않은 공통 설정 파일(`package.json`, `tsconfig.json`, 전역 타입 등)을 임의 수정하지 않습니다.

---

## 2. 오케스트레이션 라이프사이클

```
[User Request]
       │
       ▼
1. Plan & Partition (분석 및 경계 격리)
       │
  ┌────┴─────────────────┐
  ▼                      ▼
2. Fan-Out (Worker A)   2. Fan-Out (Worker B) ... [병렬 서브태스크 실행]
  │                      │
  └────┬─────────────────┘
       ▼
3. Fan-In (병합, 빌드/테스트 검증, 충돌 해소)
       │
       ▼
4. Final Delivery & Report
```

---

## 3. 단계별 실행 프로토콜

### Phase 1: Planning & Partitioning (계획 수립)
작업 시작 전 메인 오케스트레이터는 다음 템플릿에 맞춰 실행 계획을 수립합니다:
1. **작업 목표 정의**: 전체 변경 사항 명확화
2. **독립 모듈 식별**: 상호 의존성 없이 병렬 수정 가능한 도메인/파일 분리
3. **충돌 방지 맵**:
   - `Worker A`: `src/features/moduleA/**`
   - `Worker B`: `src/features/moduleB/**`
   - `Orchestrator 전용`: 공통 인터페이스, 공유 유틸리티, 환경 설정

### Phase 2: Fan-Out Execution (병렬 분기)
서브에이전트를 호출할 때는 반드시 **구체적 범위(Scope)**와 **산출물 명세**를 프롬프트로 전달합니다.

**서브에이전트 호출 템플릿:**
```bash
claude "
역할: [작업명] 전담 Worker
범위: [수정 대상 파일/폴더 경로]
목표: [구현 또는 분석 세부사항]
제약사항:
- 할당된 경로 외의 파일은 수정하지 말 것
- 작업 완료 후 (1) 변경된 파일 목록, (2) 주요 로직 요약, (3) 단위 테스트 결과를 간결히 반환할 것
"
```

### Phase 3: Fan-In Aggregation (취합 및 통합)
모든 서브태스크가 완료되면 메인 오케스트레이터는 다음 절차를 엄격히 수행합니다:
1. **산출물 수집**: 각 서브에이전트의 결과물과 수정 내역을 대조합니다.
2. **공통 인터페이스 동기화**: 공통 모듈 연동 및 타입 불일치 사항을 수동으로 조율합니다.
3. **무결성 검증 (필수 실행)**:
   - 빌드 점검: `npm run build` (또는 해당 프로젝트 빌드 명령어)
   - 테스트 실행: `npm test` (또는 관련 검증 스위트)
   - 린트/타입 점검: `npm run lint` / `tsc --noEmit`
4. **회귀 검증**: 기존 기능에 미친 영향이 없는지 확인합니다.

---

## 4. 안전 및 충돌 방지 규칙 (Strict Rules)

- **공유 파일 단독 잠금**: 여러 Worker가 동일한 파일(예: `index.ts`, 전역 상태 스토어, 라우터 설정)을 동시에 수정하지 않도록 분리하고, 통합 시점에 Orchestrator가 직접 병합합니다.
- **조기 중단 (Fail-Fast)**: 한 Worker에서 치명적 설계 결함이나 인터페이스 충돌이 발견되면 즉시 전체 작업을 일시 중지하고 재설계합니다.
- **원자적 커밋 권장**: Fan-In 검증이 성공적으로 통과했을 때만 최종 커밋 또는 완료 상태로 전환합니다.
